import styled from 'styled-components';

const StyledWrapper = styled.section`
  h1 {
    color: #666;
    font-size: 60px;
    font-weight: bold;
    text-align: center;
  }
`;

export default StyledWrapper;
